logging:
  level:
    eu.clarin.cmdi.vlo.batchimporter: INFO
vlo:
  importer:
    api:
      # Base URL of VLO API
      base-url: http://localhost:8708
      # Timeout in milliseconds
      # timeout: 6000
    metadata-source:
#      roots: 
#        - name: 'First set'
#          path: /my/vlo/records/first
#        - name: 'Second set'
#          path: /my/vlo/records/second
